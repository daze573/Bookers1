<h1>New</h1>
  <%= form_with model: @book do |f| %>

    <p>Title</p>
    <%= f.text_field :title %>

    <p>Body</p>
    <%= f.text_area :body %>

    <div class="btn">
      <%= f.submit 'Create Book' %>
    </div>
  <% end %>
